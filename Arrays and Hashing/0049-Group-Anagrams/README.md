code-1
```
1. use mapping to store key and the strings belonging 
2. store in a vector array from mapping
use Auto iterator from map.begin() to map.end()

Time Complexity : O(n*l)
Space Complexity : O(n*l)
```

