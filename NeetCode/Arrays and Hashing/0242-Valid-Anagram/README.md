code-1
```
1. Sort s and t
2. compare string
Time Complexity : Theta(nlog(n))
Space Complexity : Theta(log n)

```

code-2
```
1. Use 2 unordered Map <char,int>
2. in a loop increment the int values for each characters for both maps
3. check if both map elements are same in a loop.

Time Complexity : O(n)
Space Complexity : O(1)
```
