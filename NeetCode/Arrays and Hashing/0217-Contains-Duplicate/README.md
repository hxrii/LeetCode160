code-1
```
1. Sort 
2. check if any adjacent pairs exist

Time Complexity : Theta(nlog(n))
Space Complexity : Theta(log n)
```

code-2
```
1. Use unordered Set
2. in a loop check if s.find() != s.end()
3. if not found insert()

Time Complexity : O(n)
Space Complexity : O(1)
```
